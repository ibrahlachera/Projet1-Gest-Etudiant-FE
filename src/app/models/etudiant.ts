export interface Etudiant {
    _id?: string,
    image?: File,
    cin?: string,
    cne?: string,
    fullname?: string,
    email?: string,
    filiere?: string,
    anscol?: string,
    notes?:[{
        
    }]
}
